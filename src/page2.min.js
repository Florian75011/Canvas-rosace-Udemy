let canvas=document.getElementById("canvas2"),ctx=canvas.getContext("2d");canvas.width=window.innerWidth,canvas.height=window.innerHeight;let x=0,y=0,turn=0;function draw(){requestAnimationFrame(draw),ctx.beginPath(),ctx.strokeStyle="#FF7F50",ctx.arc(x,y,100,0,2*Math.PI),ctx.stroke(),ctx.rotate(60*Math.PI/180),turn+=60,turn>7200||turn%360==0&&(x+=12,y+=12)}ctx.translate(innerWidth/2,innerHeight/2),draw();
